# ─────────────────────────────────────────────────────────────
# Virtual Assistant — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# ─────────────────────────────────────────────────────────────

# Required: shared auth token for the WebSocket gateway
# Generate a strong random token, e.g.:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VIRTUAL_ASSISTANT_TOKEN=

# ─── AI Providers (at least one required) ────────────────────

# Anthropic Claude API key (https://console.anthropic.com)
ANTHROPIC_API_KEY=

# OpenAI API key (https://platform.openai.com/api-keys)
# Also used for text embeddings (memory feature)
OPENAI_API_KEY=

# ─── Optional overrides ───────────────────────────────────────

# Gateway port (default: 18789)
# VIRTUAL_ASSISTANT_PORT=18789

# Gateway host — keep as 127.0.0.1 for local-only access
# VIRTUAL_ASSISTANT_HOST=127.0.0.1
